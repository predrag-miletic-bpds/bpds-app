---
description: 'A bpds-prototype app.'
labels: ['app', 'react']
---

A React application for bpds-prototype. 

## Run the application

Import the app and add the following config to your workspace.jsonc:

```
bit use bpds-prototype 
```

To run the app locally, run the following:

```
bit run bpds-prototype 
```
