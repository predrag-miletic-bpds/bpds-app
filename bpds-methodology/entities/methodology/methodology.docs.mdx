---
labels: ['methodology', 'entity']
description: 'A Methodology entity.'
---

A component for a Methodology entity, supports serialization and de-serialization of Methodology objects and can be used from
both backend and frontend.

## Get started

Install and use the entity to create a new instance:

```ts
const methodology = Methodology.from({
  name: 'hello'
});
```
