export function youtubeEmbedUrl(raw?:string){if(!raw)return null;try{const u=new URL(raw);let id='';if(u.hostname.includes('youtu.be'))id=u.pathname.split('/').filter(Boolean)[0]||'';else if(u.hostname.includes('youtube.com')){if(u.pathname==='/watch')id=u.searchParams.get('v')||'';else{const parts=u.pathname.split('/').filter(Boolean);if(['shorts','embed','live'].includes(parts[0]))id=parts[1]||''}}return id?`https://www.youtube.com/embed/${id}?rel=0&playsinline=1`:null}catch{return null}}
export function directVideoUrl(raw?:string){if(!raw)return null;const clean=raw.split('?')[0].toLowerCase();return ['.mp4','.webm','.mov','.m4v'].some(ext=>clean.endsWith(ext))?raw:null}
