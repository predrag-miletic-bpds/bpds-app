import React,{useMemo,useState} from 'react';
import { directVideoUrl,youtubeEmbedUrl } from './video';
import { BpdsLogo } from './logo';
import { BPDS_APP_VERSION } from './app-version';
const DEFAULT_VIDEO='https://www.w3schools.com/html/mov_bbb.mp4';
export function PracticeMode(){const [url,setUrl]=useState(DEFAULT_VIDEO);const yt=useMemo(()=>youtubeEmbedUrl(url),[url]);const direct=useMemo(()=>directVideoUrl(url),[url]);return <main className="page"><header className="top"><BpdsLogo/><div><b>Basketball Player Development System</b><small>Practice Mode · {BPDS_APP_VERSION}</small></div></header><section className="card"><div className="eyebrow">BALL HANDLING</div><h1>Pound Dribble</h1><p>Video renderer accepts YouTube watch links, youtu.be links, Shorts and direct MP4/WebM/MOV files.</p><label className="label">Drill video URL</label><input className="input" value={url} onChange={e=>setUrl(e.target.value)} /><div className="video">{yt?<iframe src={yt} title="BPDS drill video" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowFullScreen/>:direct?<video src={direct} controls playsInline preload="metadata"/>:<div className="empty">Unsupported or missing video URL</div>}</div><div className="status">✓ Practice Mode video compatibility fix active</div></section></main>}
