---
labels: ['people', 'entity']
description: 'A People entity.'
---

A component for a People entity, supports serialization and de-serialization of People objects and can be used from
both backend and frontend.

## Get started

Install and use the entity to create a new instance:

```ts
const people = People.from({
  name: 'hello'
});
```
