---
labels: ['practice', 'entity']
description: 'A Practice entity.'
---

A component for a Practice entity, supports serialization and de-serialization of Practice objects and can be used from
both backend and frontend.

## Get started

Install and use the entity to create a new instance:

```ts
const practice = Practice.from({
  name: 'hello'
});
```
